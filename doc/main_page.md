# ROS2 C++/CUDA Template

This Doxygen page documents the reusable core library and the ROS2 package boundary. The core package is the primary API surface for non-ROS consumers; ROS2 node packages adapt that API to lifecycle/composable node workflows.

